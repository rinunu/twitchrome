
/**
 * 未読管理を行う
 */
tw.Unread = function(){
};

/**
 * 未読か判定する
 */
tw.Unread.prototype.unread = function(status){
};

/**
 * 未読・既読を設定する
 */
tw.Unread.prototype.setUnread = function(status, unread){
};

/**
 * 新規に Status が追加になった際に呼び出す
 */
tw.Unread.prototype.addStatuses = function(statuses){
};

// ----------------------------------------------------------------------

